#!/bin/bash

# Default workshop project ID
# Can be overridden by setting WORKSHOP_PROJECT_ID before running
export WORKSHOP_PROJECT_ID="eoapiworkshop"

# AWS configuration for public data access
export AWS_NO_SIGN_REQUEST=TRUE

echo "Workshop project: $WORKSHOP_PROJECT_ID"
echo "Configuration will be fetched when notebooks are opened."

exec "$@"
